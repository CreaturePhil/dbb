{
  "name": "db",
  "version": "0.0.0",
  "description": "An easy to use database.",
  "author": "Philip La <birkal@outlook.com>",
  "main": "lib/index.js",
  "scripts": {
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha -- -R spec --timeout=10000",
    "test-ci": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --timeout=10000",
    "test": "grunt && mocha --reporter=spec --timeout=10000"
  },
  "keywords": [
    "web",
    "database",
    "json",
    "easy",
    "simple",
    "simplicity"
  ],
  "license": "MIT",
  "repository": "creaturephil/db",
  "dependencies": {
    "uuid": "^2.0.1"
  },
  "devDependencies": {
    "chai": "^3.0.0",
    "grunt": "^0.4.5",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-jshint": "^0.11.1",
    "grunt-jscs": "^1.6.0",
    "istanbul": "^0.3.14",
    "load-grunt-tasks": "^3.1.0",
    "mocha": "^2.2.5"
  },
  "engines": {
    "node": ">= 0.10"
  }
}
